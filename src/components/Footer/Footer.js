import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.footer}>
      <h3>Practicing React concepts!</h3>
    </footer>
  );
};

export default Footer;
